{
  "name": "quipowallet-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ quipowallet/gasless-wallet",
    "create-local": "graph create --node http://localhost:8020/ quipowallet/gasless-wallet",
    "remove-local": "graph remove --node http://localhost:8020/ quipowallet/gasless-wallet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 quipowallet/gasless-wallet"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "0.31.0"
  }
}

